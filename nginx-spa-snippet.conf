# Paste inside your server { ... } block for the FRONTEND site (not the API).
# Without this, refreshing https://your-domain/student/dashboard returns 404
# because nginx looks for a real folder "student" instead of serving index.html.

location / {
  try_files $uri $uri/ /index.html;
}
